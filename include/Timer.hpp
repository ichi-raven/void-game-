#pragma once

#include <Siv3D.hpp>
#include <cstdint>

/*
class Timer
{
public:
    Timer();
    Timer(uint64_t limit);

    void setLimit(uint64_t limit);
    void getLimit() const;

    void setNow(uint64_t now);
    void getNow() const;

    void update();

    void reset();

    bool finished() const;

private:
    uint64_t mLimit;
    uint64_t mNow;
};*/